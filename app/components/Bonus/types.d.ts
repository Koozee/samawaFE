export type TBonusPackage = {
    id: number,
    bonusPackage: {
        id: number,
        name: string,
        slug: string,
        thumbnail: string,
        about: string,
        price: number,
    }
}